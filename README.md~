# evannsmc.com

## Publishing

Use the `publish` script to render the site and push to git:

```bash
# Default commit message ("update site")
./publish

# Custom commit message
./publish -m "your commit message"
```
